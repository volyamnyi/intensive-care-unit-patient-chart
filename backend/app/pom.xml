<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.superhumans</groupId>
        <artifactId>patient-chart-backend</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>app</artifactId>
    <name>app</name>
    <description>Patient Chart - Deployable Application Shell (assembles common + feature modules)</description>

    <dependencies>
        <dependency>
            <groupId>com.superhumans</groupId>
            <artifactId>common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.superhumans</groupId>
            <artifactId>icu-chart</artifactId>
        </dependency>
        <dependency>
            <groupId>com.superhumans</groupId>
            <artifactId>medication-sheet</artifactId>
        </dependency>
        <dependency>
            <groupId>com.superhumans</groupId>
            <artifactId>prosthesis-manufacturing</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.superhumans.IcuPatientChartApplication</mainClass>
                    <layout>JAR</layout>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>