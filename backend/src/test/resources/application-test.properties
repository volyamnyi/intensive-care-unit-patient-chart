# Test profile: override signing window to allow signing at any time
app.scheduling.signing-window-start=0
app.scheduling.signing-window-end=23

# Disable scheduled tasks in tests
app.scheduling.enabled=false

# Use lazy JPA initialization to speed up tests
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
